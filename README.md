# myKid GitHub Pages demo

Upload `index.html` to a new GitHub repository and enable GitHub Pages from the `main` branch and root folder.

This static demo stores data only in the browser's local storage. It is not a secure family-data application and must not be used for real financial or private child information.
